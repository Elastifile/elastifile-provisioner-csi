apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: demo-clone-pvc
spec:
  dataSource:
    name: demo-pvc
    kind: VolumeSnapshot
    apiGroup: snapshot.storage.k8s.io
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: elastifile
